---
name: "valuz-handbook"
description: "---"
tags: ["official", "reference"]
---

# valuz-handbook

---
name: valuz-handbook
description: Valuz 产品使用手册——核心概念、常见操作、最佳实践、官方资源（官网 / GitHub）。当用户问 Valuz 怎么用、如何创建任务/项目/Agent、如何配置模型/技能/连接器，或问官网 / GitHub / 源码地址时使用。
version: 6
tags: [official, reference]
---

# Valuz 使用手册

这是 Valuz 的产品使用手册，供「Valuz 小助手」回答「这个产品怎么用」时参考。
内容以这里为准；手册没覆盖的细节，老实说「不确定」，别编造。

## 1. Valuz 是什么

Valuz 是一个 Agent（智能体）协作平台。核心心智模型是：

> **项目 = 一个 Agent 团队协作的工作区。**

你把一批各有专长的 Agent「派驻」进一个项目，它们就能在这个项目里分工协作、
推进任务。需要随手问点什么、不想建项目时，用「临时对话」就够了。

## 2. 核心概念

- **Agent（智能体）**：可配置的执行单元。每个 Agent 有：
  - **指令（instructions）**：它的人设和工作方法（system prompt）。
  - **模型通道**：运行时（runtime）+ 模型 + 推理强度（effort）。
  - **技能（skills）**：可插拔的能力模块（如写作、检索）。
  - **装备（connectors / MCP）**：外部工具连接器。
- **Agent 库**：你拥有的所有 Agent 的集合（左侧「智能体」入口）。
- **项目（project）**：一个工作区。把库里的 Agent **派驻** 进来即可参与该项目。
- **派驻（deploy）**：把库里的一个 Agent 引用进某个项目（是引用，不是复制；
  改这个 Agent 会影响所有派驻它的项目）。
- **临时对话**：不绑定项目的快速对话，适合随手问、轻量任务。
- **会话（session）**：一次具体的对话/任务执行记录。

## 3. 常见操作（给步骤 + 入口）

- **配置模型通道**：设置 → 模型。连接一个 Provider（Claude / OpenAI / API Key 等），
  选一个默认模型。没配模型，任何 Agent 都跑不起来。
- **创建 Agent**：「智能体」页右上角「+」→ 填名称、一句话定位、模型，
  写指令；技能/装备按需在「增强能力」里加。创建后可在详情页继续微调。
- **复制现有 Agent**：在「智能体」列表里，对目标 Agent hover →「···」→「复制」，
  它的配置会带进创建表单，改改即可。
- **创建项目**：「项目」页新建一个项目（绑定一个本地目录作为工作区）。
- **把 Agent 派驻进项目**：在 Agent 详情页点「派驻到项目」选一个项目；
  或在项目里「添加智能体」从库里选。
- **开始临时对话**：首页「新对话」，在输入框上方选一个 Agent，直接聊。
- **给 Agent 加技能/装备**：Agent 详情页 →「技能」/「装备」标签 →「添加」。
- **接 MCP 连接器**：设置 / 连接器里配置 MCP server，再在 Agent 的「装备」里挂上。

## 4. 定时任务 / 自动化（重要：用 `automation` MCP 工具）

> ⚠️ **强调**：凡是涉及「定时」「每天 / 每周 / 每隔 N 分钟」「到点自动跑」「自动执行某指令」
> 的需求，**必须直接调用 `automation` MCP 工具来完成，不要只用自然语言答复用户、
> 也不要引导用户手动去界面里配**。这是 Valuz 创建 / 管理自动化的唯一正确入口。

`automation` 工具是多动作工具，`action` 取值：

- `create`：**提议**一条自动化（必填 `name`、`prompt_template`、`trigger`；`agent_slug` 视上下文）。
  ⚠️ **`create` 不会直接落库**——它会在对话里渲染一张确认卡片，由用户点击「创建」后才真正生成
  （与 `propose_agent` 同一套「工具提议、用户确认」机制）。所以**只调用一次** `create`，用自然语言
  复述你拟定的时间安排，然后**停下来等用户确认**，不要重复调用、也不要假定它已经建好了。
- `list`：列出自动化。
- `update` / `pause` / `resume` / `run` / `remove`：按 `automation_id` 改 / 暂停 / 恢复 / 立即跑一次 / 删除。

关键参数：

- **`trigger`（触发器，多态）**——「每隔 N 分钟 / 秒」用 interval，整点 / 固定时刻用 cron：
  - 定时表达式 → `{"kind": "cron", "cron_expr": "0 9 * * *"}`（如每天 9 点）。
  - 固定间隔 → `{"kind": "interval", "seconds": 3600}`（每 3600 秒，最小 30；注意键名是 `seconds`，不是 cron）。
  - 手动触发 → `{"kind": "manual"}`。
- **`agent_slug`**：执行身份。项目会话里必须是该项目的成员 Agent；临时对话里用一个库 Agent
  （不指定时会默认用当前对话绑定的 Agent）。
- **`action_kind`**：执行模式。`chat`（默认）= 每次触发让绑定 Agent 单轮跑一次；`task` = 以绑定
  Agent 作为 **Lead** 发起一个项目任务。`task` **只能在项目会话里创建**（需要项目的任务上下文）；
  在临时对话里会被拒绝——那里省略或用 `chat`。
- **`scope`**：`this` = 仅当前项目；`all` = 整个用户库（仅在临时对话里生效）。一般可省略，
  用默认即可。

约定：

- 用户说「每天早上拉行业数据」「每周五生成报告」这类话，先把它转成
  `create` 调用（拟定 `name` + `prompt_template` + 合适的 `trigger` + `agent_slug`），
  调用后向用户复述这条自动化、并提示在卡片上点「创建」确认。
- 用户问「我有哪些定时任务 / 自动化」→ 直接 `action: list`。
- 改时间、暂停、删除 → 先 `list` 拿到 `automation_id`，再对应 `update` / `pause` / `remove`。

## 5. 最佳实践

- **临时对话 vs 项目**：随手问、一次性的小任务 → 临时对话；需要多角色协作、
  反复推进、留痕的工作 → 建项目并派驻一个团队。
- **遇到专业深度任务**（投研、写作、产研等）：别用通用助手硬扛，
  在项目里派一个更专业的 Agent，或从预设团队里选。
- **模型 / 推理强度**：日常问答用默认即可；需要深度推理的复杂任务调高 effort。
- **成果物必须交付（强规则）**：结束本轮回答前，先判断产出里是否包含「成果物」——
  任何对用户有留存价值、值得被保存 / 再次打开 / 转发 / 导出的实质产出，而不只是对话里的即时文字。
  判断锚点是产出的**性质**（可复用、可留存、可分享），而非具体格式；典型形态包括但远不止
  报告、文档、表格、图表、演示文稿、代码、数据集、网页或各类导出文件。一旦判定存在成果物：
  1. 先把它落成本地文件（用合适的格式写到磁盘）；
  2. 调用 `deliver_artifacts`，把文件路径登记进用户的「生成文件」面板；
  3. 最终回复保持简洁，说明交付了什么即可，不必把成果物内容整段复述。

  把 `deliver_artifacts` 当作交付实质产出的**必经步骤**，而不是「文件写完后可有可无的收尾动作」。
  只交付最终成品，中间草稿和临时文件不必登记。仅当本轮明显只是简短问答、澄清、状态同步，
  或一次性、不需要留存的讨论时，才跳过这条规则。

## 6. 官方资源

- **官网**：https://valuz.io
- **开源仓库（GitHub）**：https://github.com/valuz-ai/valuz-oss

当用户问「官网 / 官方网站 / 在哪下载 / 源码 / GitHub / 开源地址」时，给上面对应的链接。

## 7. 边界

手册没写到的产品细节，不要猜测或编造。引导用户去对应的设置页自己确认，
或老实回答「这个我不确定」。
