#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Sidecar build orchestration placeholder."
echo "Expected sidecar roots:"
echo "  ${ROOT_DIR}/sidecar-services/agent-server"
