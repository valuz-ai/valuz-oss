// __EDITION__ 由 Vite/esbuild 在编译时通过 define 注入。
// 类型声明为 string——公共骨架不写死任何 edition 字面量；
// 默认值 'personal' 由 build 配置兜底。
// eslint-disable-next-line @typescript-eslint/no-unused-vars
declare const __EDITION__: string;

export {};
