vendored_from: agent-harness
upstream_path: /Users/river/Documents/xb_workspace/agent-harness
upstream_remote: git@code.xiaobangtouzi.com:backend/agent-harness.git
commit: eec331f
commit_subject: Merge feat/codex-pypi-bundled-bin into main (codex from PyPI + dynamic workflows)
core_version: V5
vendored_at: 2026-06-04

# Notable changes since fdb25a3 (previous pin) — dynamic workflows + codex PyPI
#   eec331f  Merge feat/codex-pypi-bundled-bin into main.
#   * ac486f5 feat(codex): install openai-codex from PyPI and prefer bundled
#     cli-bin. Switched the codex runtime off the vendored 0.131.0a4 wheel +
#     override-dependencies to the published ``openai-codex`` beta (0.1.0b2).
#       - API rename: ``AppServerConfig`` -> ``CodexConfig`` (runtime + tests).
#       - ``_resolve_codex_bin``: CODEX_BIN_OVERRIDE > bundled
#         ``codex_cli_bin.bundled_codex_path()`` (lazy, ImportError-guarded) >
#         PATH ``codex`` > None.
#     ★ HOST IMPACT: host moved ``openai-codex==0.131.0a4`` (vendored wheel) ->
#       ``openai-codex`` (PyPI beta, resolves 0.1.0b2 + cli-bin 0.132.0) for the
#       ``CodexConfig`` import, MIRRORING upstream — i.e. host DROPPED its old
#       ``[tool.uv] override-dependencies`` so uv pulls ``openai-codex-cli-bin``
#       (the per-platform, version-pinned codex binary). ``_resolve_codex_bin``
#       now prefers that pinned binary over a stray PATH ``codex`` (the whole
#       point of ac486f5). Codex stays dormant in valuz, but we no longer
#       diverge. Obsolete ``backend/vendor/openai_codex-0.131.0a4`` wheel removed.
#       (Upstream also added ``codex_cli_bin`` to its mypy ignore-missing-imports
#       list; host needs no equivalent — host mypy ``exclude=["^kernel/"]`` +
#       ``follow_imports=skip`` for ``src.*`` means it never type-checks the
#       codex runtime that imports ``codex_cli_bin``.)
#   * 67ed9e0 feat: enable dynamic workflows + live run progress (Claude
#     ``Workflow`` tool). New OutboundEventType ``workflow_progress`` (live-only,
#     in ``DatabaseEventSink._NON_PERSISTED_TYPES``); claude_agent runtime polls
#     the run's ``wf_<id>.json`` and streams phases/agent progress.
#     ★ HOST IMPACT: none required — ``event_sse_adapter`` drops unmapped kernel
#       types gracefully and ``workflow_progress`` is non-persisted (the DB-row
#       SSE path never sees it). Surfacing live workflow progress in valuz UI is
#       a future product feature, not an upgrade requirement.
#   * d61e0ba chore(deps): bump claude-agent-sdk to 0.2.89 (bundled Claude Code
#     CLI for the ``Workflow`` tool). Host floor bumped 0.2.83 -> 0.2.89.
#   ★ SCHEMA: no new alembic revision since the pin — no kernel-table rebuild
#     fingerprint needed.

# Notable changes since 3e742fc (older pin) — timestamps as Unix epoch ms
#   fdb25a3  Merge fix/timestamps-epoch-millis into main.
#   * 4c6c50d fix: store timestamps as Unix epoch milliseconds (UTC). Every
#     instant in the kernel is now a single ``int`` (Unix epoch ms, UTC),
#     end-to-end: domain dataclasses, DB (plain BIGINT), and API wire. New
#     ``src/core/time_utils.py`` (``now_ms()`` — stdlib ``time`` only).
#       - core types: Project.created_at, AgentConfig.created_at,
#         Session.created_at, Message.started_at/ended_at, Event.timestamp
#         all ``datetime`` → ``int``.
#       - models.py: those columns ``DateTime`` → ``BigInteger``; dropped the
#         ``server_default=func.now()`` (app sets now_ms()).
#     ★ SCHEMA: new alembic revision ``e7f1a2b3c4d5`` (down ``b1c2d3e4f5a6``,
#       the prior head) batch-alters DateTime → BigInteger on agents/projects/
#       sessions/messages/events. SCHEMA-ONLY: existing rows are NOT converted
#       (old datetime data → garbage ints). Dev policy: rebuild / fresh DB.
#       A new data dir comes up clean; an existing dev DB should be wiped
#       (or accept garbage timestamps on pre-bump rows — display only).
#     ★ HOST IMPACT: kernel timestamps are now ``int`` ms and the host passes
#       them STRAIGHT THROUGH to its DTOs + API wire (no datetime conversion);
#       the frontend formats via ``new Date(ms)``:
#         · modules/sessions/dto — SessionListItem.updated_at /
#           SessionDetail.created_at / SessionEventEnvelope.timestamp → ``int``.
#         · modules/sessions/mappers — session.created_at int passes through.
#         · adapters/event_sse_adapter — SessionEventFrame.timestamp ``int``;
#           to_sse_data emits the raw int (no isoformat).
#         · modules/sessions/service — listEvents envelope timestamp = int.
#         · modules/analytics/{datastore,service} — kernel MessageModel.
#           started_at is BIGINT ms: day bucket via
#           ``strftime('%Y-%m-%d', started_at/1000, 'unixepoch')``; range
#           filter compares epoch-ms bounds (service computes start_ms/end_ms).
#       Contract: api/openapi.yaml session timestamp fields → integer/int64.
#       Frontend: shared session/conversation types + their consumers switched
#       from ISO string to number (new Date(ms)). ╳ NO new dependency.

# Notable changes since cb25177 (previous pin) — in-flight session status
#   3e742fc  Merge feat/session-status-in-flight into main.
#   * 184b653 feat(orchestrator): persist session.status="running" +
#     scan_orphan_runs. ``run_turn`` now writes ``session.status="running"``
#     to the DB (save_session) right after save_message, BEFORE handing off
#     to the runtime — previously "running" was only an in-memory flag the
#     runtime set at run() entry, saved back as "idle" at turn end, so the DB
#     never observed a real running row. A ``finally`` defensive reset forces
#     status back to "idle" if run() returned without restoring it (host
#     crashes / SIGKILL skip this branch → leave an orphan row). New
#     ``SessionOrchestrator.scan_orphan_runs()`` (symmetric with
#     scan_orphan_pendings) resets sessions left "running" after a crash to
#     "idle" + stop_reason host_restart, and flips any Message.status="running"
#     row to "errored" so history reads don't render a perpetual spinner.
#     Wired into app/dependencies.py::init_dependencies.
#     ★ HOST IMPACT: removes the need for the host-side ``_persist_running``
#       workaround in modules/tasks/orchestrator.py — the kernel now owns
#       in-flight running persistence. Workaround dropped in the same commit.
#   * efeaa10 / 2f60687 docs(api): expose K-PR2 agent.runtime_provider +
#     K-PR3 session.cwd via kernel OpenAPI (app/routes/sessions.py +
#     app/schemas.py). Host has its own api/openapi.yaml — no regeneration
#     needed. NO schema (alembic/models) change, NO new Python dependency.

# Notable changes since e57531a (previous pin) — Session Modes feature
# (docs/design/session-modes.md), shipped on top of the already-vendored
# K-PR1/2/3 host-orchestration primitives:
#   cb25177  Merge feat/exec-ctx-and-runtime-provider into main.
#   * NEW: Session.mode enum ``default | plan | goal`` (core/types.py) +
#     sessions.mode column (models.py String(20) default "default",
#     CHECK mode IN ('default','plan','goal')) + converters both ways.
#     ★ SCHEMA: new alembic revision ``4b2490e2b9c4`` (down ``4539c7fc4d6a``)
#       INSERTED MID-CHAIN — ``90473e68fdf1`` down_revision repointed
#       4539c7fc4d6a → 4b2490e2b9c4. A dev DB stamped at the old tail
#       (b1c2d3e4f5a6) will NOT backfill sessions.mode on ``upgrade head``.
#       HOST IMPACT: m000_legacy_dropout gains a column-presence fingerprint
#       (sessions lacks ``mode`` → drop+rebuild 4 kernel tables). Pre-launch
#       policy: no patch migration, rebuild + self-heal.
#   * API: POST /api/v1/sessions/{id}/mode (enter/switch/exit). Host passes
#     through via kernel_sync; no host business logic.
#   * BEHAVIOR: prompt_builder.wrap_for_mode() wraps the post-switch user
#     message into ``/goal <condition>``; orchestrator syncs session.mode
#     across turns (runtime_mode_change); claude_agent lowers mode=="plan" →
#     SDK permissionMode="plan", mode=="goal" → Claude /goal loop (Haiku
#     evaluator Stop-hook), auto-exits to "default" on completion. codex
#     lowers to its native plan/goal protocols. deepagents → route 400.
#   * EVENTS: new outbound ``mode_changed`` + ``plan_update`` (core/events.py).
#     HOST IMPACT: event_sse_adapter must allow these through.
#   ╳ NO new Python dependency.
#
# ---- prior pin history (kept for context) ----
# Notable changes since beae4ae (e57531a pin):
#   e57531a  feat(sessions): add Session.cwd  [K-PR3]
#            * Session.cwd (core/types.py) + sessions.cwd column
#              (models.py, Text default "") + converters both directions.
#            * orchestrator.run_turn passes ``session.cwd or project.cwd``
#              as the runtime workspace_root — per-session cwd override,
#              empty falls back to project.cwd.
#            ★ SCHEMA: new alembic revision ``b1c2d3e4f5a6`` (down
#              ``90473e68fdf1``) adds sessions.cwd. Normal upgrade chain.
#            ╳ NO new dependency. HOST IMPACT: host may now set
#              session.cwd when building a Session (per-task/run isolation,
#              lead-dispatch-mvp §3); empty preserves existing behaviour.

# Notable changes since 2ffdd86 (kept for context):
#   beae4ae  Merge — exec-context-session-id + agent-runtime-provider.
#   59adf33  feat(agents): add runtime_provider to the agent template  [K-PR2]
#            * AgentModel/AgentConfig 加 ``runtime_provider`` 列
#              (``String(20)``, default ``"claude_agent"``) —— agent 级
#              「默认值」,与既有的 SESSION 级 ``runtime_provider``
#              (创建时选定、不可变) 区分。converters + app/routes/agents.py
#              + app/schemas.py 同步。
#            ★ SCHEMA: 新 alembic revision ``90473e68fdf1`` (down_revision
#              ``4539c7fc4d6a``) —— 正常 alembic upgrade 链,无 revision-id
#              复用,无需 m000 重建指纹。现有 dev DB 会自动 upgrade 上去。
#   64daafe  feat(tools): expose calling session identity via ExecContext  [K-PR1]
#            * ``ExecContext`` 加 ``session_id: str = ""``;3 个 runtime
#              (claude_agent/codex/deepagents) 在 run() 作用域填值。
#            ╳ NO schema change. ╳ NO new dependency (column + stdlib only).
#
#   HOST-SIDE IMPACT: 本次升级**纯加法**,host 无需改动即可继续工作 ——
#     新 ``agents.runtime_provider`` 有 default,既有 AgentConfig 构造点
#     不传也不破。读取 ``agent.runtime_provider`` 写入 session 是后续
#     host 工作 (agent-project-task-architecture §6 resolver / H5),不在
#     本次范围。边界守卫 (no ``from src.adapters/runtimes`` 越界) 仍空。
#
#   K-PR3 (sessions.cwd, per-task/sub-run 文件隔离) 上游**尚未实现** ——
#     types.py 仍是 "cwd derived from parent project's cwd",无 session.cwd
#     列。需另起上游 PR 后再 re-vendor。

# Notable changes since 9e29f15 (kept for context):
#   2ffdd86  Merge — skills materialize via symlink instead of copytree.
#   2f8b938  feat(skills): materialize via symlink instead of copytree.
#            * ``runtimes/skills_materialize._materialize`` now writes
#              POSIX symlinks pointing at the absolute source dir
#              (``os.symlink(os.path.abspath(src), dst,
#              target_is_directory=True)``) instead of recursive
#              ``shutil.copytree``. Two observable effects:
#                1. Edits to source skill files reach the running
#                   runtime immediately — no re-materialize / no
#                   session restart needed (under copytree the subtree
#                   was a frozen snapshot until next session start).
#                2. O(N entries) syscalls instead of O(total file
#                   bytes) of I/O; no disk-space duplication.
#            * Cleanup helper ``_remove_managed_entry`` only acts on
#              symlinks (``os.path.islink`` -> ``os.unlink``).
#              ``islink`` is True for broken symlinks too, so cleanup
#              stays idempotent after the source was deleted. A
#              non-symlink at the target path is left alone; the next
#              ``os.symlink`` raises ``FileExistsError`` — loud and
#              visible, easier to debug than silent partial state.
#            * ``shutil`` import dropped from the module.
#
#   ╳ NO schema change. Alembic head stays at 4539c7fc4d6a.
#   ╳ NO new kernel dependency. ``os.symlink`` is stdlib.
#   ╳ NO public API surface change (no schemas.py / routes/* edits,
#      no ``src/core/__init__.py`` re-export deltas). The internal
#      ``materialize_skills_for_session`` signature is unchanged.
#
#   ╳ HOST-SIDE IMPACT: zero code changes required.
#      * The kernel self-manages its materialized subtree under each
#        ``project.cwd/.claude/skills/`` (Claude) and ``.agents/skills/``
#        (codex / deepagents). Per ADR-004 the host never reads from or
#        writes to those locations — every host reference to
#        ``.claude/skills`` / ``.agents/skills`` in valuz is the user's
#        SOURCE skill root (``~/.agents/skills/`` etc.), not the kernel's
#        per-session materialized copy. Confirmed via grep across
#        ``valuz_agent/`` — no touch points on the materialized subtree.
#      * Symlink semantics mean a future host-side cleanup tool that
#        ever walks ``project.cwd/.claude/skills/`` MUST use ``os.unlink``
#        / ``Path.unlink()``, not ``shutil.rmtree`` — but no such tool
#        exists today and the kernel's own cleanup already handles it.
#      * ``infra/fs_registry`` already documents the kernel ownership in
#        its docstring — no change needed.

# Notable changes since bba3014 (previous pin):
#   9e29f15  Merge — codex reasoning thinking events end-to-end.
#   d95d539  docs(changelog): record codex reasoning thinking events fix.
#   89384b5  feat(codex): surface reasoning thinking events end-to-end.
#            * config_overrides always emits ``model_reasoning_summary=
#              "auto"`` (codex CLI's hidden default is effectively
#              ``none`` — without this, o-series / gpt-5 silently
#              produced zero thinking events). System-level constant,
#              not user-configurable. No thread-metadata pin trap for
#              summary (ThreadStart/Resume params don't expose it).
#            * event_mapper handles 2 new notification types as
#              ``thinking_delta``:
#                ReasoningSummaryTextDeltaNotification → delta text.
#                ReasoningSummaryPartAddedNotification → "\n\n"
#                paragraph break (no payload field).
#   47f6182  Merge — WebSearch disable on non-subscription mode.
#   4877503  docs(changelog): record WebSearch disable (994c9cd).
#   994c9cd  feat(runtimes): disable WebSearch tool when
#            session.model_provider is set.
#            * ClaudeAgentRuntime: emits ``ClaudeAgentOptions.
#              disallowed_tools=["WebSearch"]`` (SDK hard-deny list,
#              NOT the approval-bypass ``allowed_tools``).
#            * CodexRuntime: emits ``web_search="disabled"`` config
#              override (WebSearchMode enum: disabled/cached/live;
#              top-level key, not nested under [tools]).
#            * Gate is ``provider is not None`` regardless of
#              ``base_url`` — first-party fallback (api_key only,
#              SDK ambient endpoint) is still non-subscription mode.
#            * Reference docs updated: claude-agent-options-and-mutators.md
#              ``disallowed_tools`` row + codex-thread-vs-turn-kwargs.md
#              ``web_search`` row.
#
#   ╳ NO schema change. Alembic head stays at 4539c7fc4d6a.
#   ╳ NO new kernel dependency.
#   ╳ NO public API surface change (no schemas.py / routes/* edits).
#
#   ╳ HOST-SIDE IMPACT: zero code changes required.
#      * valuz_agent.adapters.provider_resolver.resolve_model_provider
#        already returns ``None`` for ``auth_type=="oauth"`` providers
#        (Claude Pro/Max + Codex subscription). The new WebSearch gate
#        (``model_provider is not None``) therefore fires correctly:
#          - subscription session  → model_provider=None → WebSearch on
#          - api_key session       → model_provider set  → WebSearch off
#      * valuz_agent.adapters.event_sse_adapter already translates
#        kernel ``thinking_delta`` → SSE ``message.assistant.thinking_delta``;
#        frontend chat-store + conversation-utils already accumulate
#        these into the thinking buffer. New codex reasoning summary
#        events flow through the same pipeline transparently.

# Notable changes since d008b53 (previous pin):
#   bba3014  Merge — effort + api_protocol expansion track. Aggregates the
#            two feature commits below.
#   e319002  docs(changelog): record per-session model levers track
#            (f1c0539 + da025d6).
#   a69fc81  Merge — feat(sessions): expand api_protocol enum +
#            ModelSettings.effort (intermediate).
#   da025d6  feat(sessions): live-reconcile permission_mode + effort
#            across runtimes; base_url optional. UI exposes
#            PermissionModeSelector + EffortSelector beside chat input;
#            PATCH session applies on next Send.
#            * Claude: client.set_permission_mode for safe transitions;
#              bypassPermissions/dontAsk short-circuits to destroy+rebuild
#              with one-shot _fork_next_spawn (G1/G2 CLI gotchas).
#            * Codex: _build_turn_kwargs(session) reads session live and
#              emits per-turn approval_policy + sandbox_policy +
#              reasoning_effort.
#            * DeepAgents: _reconcile_session_levers drops self._graph
#              when permission_mode or effort drifted from _applied_*.
#            * ModelProvider.base_url Optional — empty/whitespace
#              normalizes to None; all 3 runtimes fall back to SDK ambient
#              endpoint (ANTHROPIC_AUTH_TOKEN / OPENAI_API_KEY env).
#   f1c0539  feat(sessions): expand api_protocol enum + add
#            ModelSettings.effort.
#            * api_protocol [openai, anthropic] → [anthropic,
#              openai_completion, openai_response, gemini].
#            * Per-runtime allowlist in factory.ALLOWED_PROTOCOLS_BY_RUNTIME
#              (route 400 + factory ValueError defense-in-depth):
#                claude_agent → {anthropic}
#                codex        → {openai_response}
#                deepagents   → {anthropic, openai_completion, gemini}
#            * ModelSettings.effort: low|medium|high|xhigh|max. Per-runtime
#              mapping: Claude pass-through, Codex (max→xhigh),
#              DeepAgents openai_completion (max→xhigh) / anthropic
#              (pass-through) / gemini (xhigh|max→high).
#            * Claude runtime no longer threads thinking=; SDK uses its own
#              default (per 2026-05-12 directive).
#            * Frontend: EffortSelector (ChatGPT-style dropdown beside chat
#              input), runtime-protocols.ts mirrors per-runtime allowlist,
#              New Session dialog auto-picks default api_protocol on
#              runtime change.
#
#   ╳ BREAKING (schema): alembic head pushed from 807642401b71 to
#      4539c7fc4d6a. Pure JSON-content migration (no DDL): rewrites
#      sessions.model_provider.api_protocol from legacy "openai" to
#      "openai_completion" (deepagents) or "openai_response" (codex)
#      based on each row's runtime_provider. Reversible. effort lives
#      inside model_settings JSON blob — no DDL needed.
#
#   ╳ NEW API SHAPES:
#      ModelProvider.api_protocol Literal extended:
#        "anthropic" | "openai_completion" | "openai_response" | "gemini".
#      ModelProvider.base_url: str | None (was str). None enables
#        first-party SDK endpoint fallback per runtime.
#      ModelSettings.effort: Literal["low","medium","high","xhigh","max"] | None.
#      Host hazard: valuz adapter provider_resolver._resolve_api_protocol
#        MUST return the 4-value form; existing 2-value collapse breaks.
#      Host hazard: valuz adapters/_resolve_base_url MUST normalize
#        empty/whitespace to None.
#      Host hazard: valuz routes session create + PATCH MUST call
#        factory.validate_api_protocol(runtime, api_protocol); mismatch → 400.
#
#   ╳ NEW SESSION FIELDS (Pydantic schemas):
#      SessionCreateRequest.effort: EffortLiteral | None (new optional).
#      SessionDetail.model_settings.effort: EffortLiteral | None (new).
#      Host hazard: valuz SessionDetail / SessionListItem schemas must
#        expose model_settings.effort end-to-end.
#
#   ╳ NEW LIVE-RECONCILE CONTRACT:
#      PATCH session.permission_mode and PATCH session.model_settings.effort
#      apply on the NEXT Send (not next cold load). Claude uses live
#      set_permission_mode mutator (or fork-on-rebuild for bypass tier);
#      Codex uses turn_kwargs layer; DeepAgents drops self._graph for
#      cold rebuild. No event schema changes — observable contract is
#      "applies on next user message".
#      Host hazard: valuz frontend Composer that hard-locked
#      permissionMode for live sessions can now be unlocked; same for
#      effort.
#
#   ╳ NEW REFERENCE DOCS (mirrored into valuz docs/references):
#      codex-thread-vs-turn-kwargs.md — 3-layer codex SDK kwargs model
#        (config_overrides → thread_kwargs → turn_kwargs); explains the
#        thread-metadata pin trap (effort survives resume only via
#        turn_kwargs).
#      claude-agent-options-and-mutators.md — 39 ClaudeAgentOptions fields
#        + 10 live mutators + four CLI gotchas:
#          G1 bypassPermissions one-shot transition gate
#          G2 --resume silent override of permission_mode
#          G3 setting_sources=["project"] excludes ~/.claude.json
#          G4 allowed_tools is approval-bypass list (NOT visibility)
#
#   ╳ NEW DEPENDENCY: langchain-google-genai>=4.2.2 (DeepAgents gemini
#      protocol path). Host pyproject must mirror to avoid mid-turn
#      ImportError if a future provider routes there.
#
#   ╳ NO NEW KERNEL CORE FILES (only edits to existing types/runtimes/
#      schemas + new alembic revision).

# Notable changes since 1aae940 (previous pin):
#   d008b53  Merge — approval contract v2 (``approve_for_session``).
#            Aggregates the four phases below + the Claude allowed_tools
#            MCP-bypass fix.
#   737a801  docs(approvals): record the Claude allowed_tools gotcha +
#            bump CHANGELOG.
#   c93b018  fix(approvals): don't bypass ``can_use_tool`` for
#            user-configured MCP servers on Claude. _build_options used
#            to append ``mcp__<server>__*`` to ``ClaudeAgentOptions.
#            allowed_tools`` for every entry in ``session.mcp_servers``;
#            allowed_tools is the SDK's approval-bypass list, so every
#            user-MCP call auto-approved upstream of ``can_use_tool``.
#            Post-fix, MCP calls correctly route through the harness
#            approval flow. ``mcp__harness__*`` and ``Agent`` stay in
#            allowed_tools (operator-defined, harness is authority).
#   9515504  feat(approvals): approve_for_session Phase 4 — per-subject
#            rule keying. ``src/core/rule_canonicalize.py::
#            reduce_args_for_subject`` reduces args per subject before
#            rule identity is computed: mcp_tool_call → endpoint
#            identity (drop all args); file_change → ``{file_path}``;
#            shell_command → ``{command, cwd}``. Fixes the
#            ``stock_quote(symbol="000858") → ...="000568"`` bug.
#   523aaee  feat(approvals): approve_for_session Phase 3 — Claude with
#            pattern grammar. ``ClaudePermissionUpdateRuleMatcher``
#            consumes ``ToolPermissionContext.suggestions``; supports
#            Bash prefix, file glob, MCP tool-name patterns. Falls back
#            to exact-match when SDK provides no proposal.
#            ``clarifying_questions`` skips cache + preview.
#   6c22668  feat(approvals): approve_for_session Phase 2 — codex.
#            ``_extract_matcher_inputs`` reduces JSON-RPC params per
#            subject. Codex SDK never sees ``AcceptForSession`` on the
#            wire — kernel cache is single source of truth.
#   87e8e22  feat(approvals): approve_for_session Phase 1 — kernel +
#            DeepAgents. Adds ``SessionApprovalCache`` (in-memory,
#            per-session, cleared on cold reload),
#            ``RuntimeApprovalRuleMatcher`` Protocol with
#            ``derive_rule`` / ``match``, default
#            ``ExactArgsRuleMatcher`` (canonical-JSON of args).
#            Orchestrator: ``approve_for_session`` → commit rule
#            kernel-side → translate to plain ``approve`` at runtime
#            boundary; emit ``action_resolved(decision=
#            "approve_for_session", rule_id=...)``. Cache hit:
#            ``action_resolved(decision="auto_approved",
#            auto_resolved_by_rule_id=...)`` — uniform event flow
#            across runtimes, no SDK-side auto-approve.
#   dda8acc  docs(approvals): approve_for_session v2 design spec.
#   b53d273  chore(approvals): ship v1 changelog and freeze v2+ track.
#   3e4c14b  feat(approvals): B2a — classify Claude tier-mismatch at
#            first turn. ``_classify_first_turn_error`` narrowly maps
#            tier/plan stderr substrings to ``Error(category=
#            "tier_mismatch", retry_status="terminal", message=
#            "recreate with default/full_access, or upgrade your
#            plan")``. Only fires for ``auto_review`` sessions. Never
#            adds false positives; only refines existing failure.
#   ee7491f  fix(approvals): style approve_with_changes as positive
#            resolution.
#   d290f88  feat(approvals): A1 — ``approve_with_changes`` verb on
#            Claude + DeepAgents. Carries ``modified_input: dict``
#            (replacement args). Claude maps to
#            ``PermissionResultAllow(updated_input=...)``; DeepAgents
#            ``EditDecision.edited_action.args`` (original tool name
#            preserved, args only). Orchestrator gates by pending's
#            ``available_decisions``; codex pendings 400 (codex SDK
#            has no analog). Payload ships ``original_input`` so the
#            frontend JSON editor can seed from full args.
#   672e5c3  feat(approvals): A2 — reject-with-redirect parity. Claude
#            ``PermissionResultDeny.message`` + DeepAgents
#            ``RejectDecision.message`` deliver rejection reason to
#            model; Codex 0.116.0a1 has no reason field, runtime
#            ``logger.info``s for operators.
#
#   ╳ NO new alembic migration. ``SessionApprovalCache`` is in-memory,
#      cleared on cold reload. ``807642401b71`` remains the head;
#      ``m000_legacy_dropout`` column fingerprint unchanged.
#
#   ╳ NEW DECISION VERBS:
#      ``approve_with_changes`` — user-submittable; payload
#        ``modified_input: dict``. Pydantic validator enforces the
#        ``approve_with_changes ↔ modified_input`` invariant symmetric
#        to ``answer ↔ answers``.
#      ``approve_for_session`` — user-submittable; commits a
#        kernel-owned session-scoped rule. Orchestrator translates to
#        plain ``approve`` at the runtime boundary; never reaches the
#        SDK.
#      ``auto_approved`` — kernel-synthesized; emitted by orchestrator
#        on cache hit without user round-trip. Pydantic validator
#        REJECTS it as ``SubmitActionRequest`` input (symmetric to
#        ``expired`` / ``interrupted``).
#
#   ╳ NEW EVENT FIELDS:
#      ``requires_action.session_rule_preview: {kind, display,
#         runtime_kind, rule_data}`` — present for tool-approval
#         subjects only (NOT clarifying_questions).
#      ``requires_action.original_input: dict`` — populated for
#         tool-approval subjects so the frontend Edit & Approve JSON
#         editor can seed from full args.
#      ``action_resolved.rule_id: str | None`` — populated when
#         ``decision == "approve_for_session"``.
#      ``action_resolved.auto_resolved_by_rule_id: str | None`` —
#         populated when ``decision == "auto_approved"``.
#      Host hazard: ``adapters/event_sse_adapter`` JSON-stringifies
#         the structured fields to honour the legacy
#         ``Record<string,string>`` SSE wire contract. Frontend
#         ``parseRequiresAction`` / ``parseActionResolved`` decode.
#
#   ╳ NEW API SHAPE (no new route):
#      ``SubmitActionRequest.decision`` Literal extended:
#        ``approve | approve_with_changes | approve_for_session |
#         reject | answer``.
#      ``SubmitActionRequest.modified_input: dict | None`` (new
#        optional field; required iff decision == approve_with_changes).
#      ``SubmitActionData.rule_id: str | None`` (new on response).
#      Host hazard: valuz host route at
#        ``POST /v1/sessions/{id}/actions`` (``api/routes/sessions.py``)
#        MUST mirror the same field/enum extensions verbatim.
#
#   ╳ BEHAVIORAL CHANGE (c93b018): Claude ``default`` /
#      ``auto_review`` mode now parks on user-configured MCP calls.
#      Pre-fix, ``mcp__<server>__*`` was in ``allowed_tools`` so the
#      Claude CLI auto-bypassed ``can_use_tool``. Valuz today defaults
#      sessions to ``full_access``, so this is opt-in; but any user
#      that's already on ``default`` will start seeing
#      ``requires_action`` cards on the next MCP call. Ship the
#      approval UI before users hit this in the wild.
#
#   ╳ NEW KERNEL CORE FILES:
#      ``src/core/session_approval_cache.py`` — per-session in-memory
#         rule store. Single instance lives on the orchestrator.
#      ``src/core/rule_canonicalize.py`` — ``reduce_args_for_subject``
#         reducer that drops irrelevant args per subject before rule
#         identity is computed (Phase 4).
#      ``src/core/approval_rule_matcher.py`` —
#         ``RuntimeApprovalRuleMatcher`` Protocol + default
#         ``ExactArgsRuleMatcher``. Each runtime ships its own
#         matcher: Claude → ``ClaudePermissionUpdateRuleMatcher``;
#         Codex → drops ``input`` from MCP key; DeepAgents → exact
#         args.

# Notable changes since 02f8184 (kept for context):
#   1aae940  Merge — cross-runtime tool approval contract v1 (Phase 1 + 2 + 3
#            shipped: Claude / Codex / DeepAgents). Aggregates the slice
#            commits below; this is the user-facing landing commit.
#   46001b9  docs(approvals): audit the design doc against shipped reality.
#   009881c  feat(approvals): handle Claude SDK AskUserQuestion (clarifying
#            questions) — adds the ``clarifying_questions`` subject + the
#            ``answer`` decision so the host can return structured option
#            selections back to the SDK without polluting the system prompt.
#   7ec7dc0  refactor(approvals): unify ``""`` as the cross-runtime "server
#            attribution unavailable" sentinel.
#   b64ba59  feat(deepagents): wire Phase 3 of the approval contract via
#            langgraph ``interrupt_on={tool: …}`` + ``Command(resume=…)``
#            in the runtime outer loop.
#   ae0982c  feat(approvals): Phase 2 — Codex approval bridge.
#   23147a0  / 811724b  refactor: extract per-runtime approval bridge into
#            its own module so the runtime files stay thin.
#   9bf4eaf  fix(approvals): render MCP elicitation card with the prompt text.
#   8c95c1f  feat(approvals): Slice 3 — ClaudeAgentRuntime approval bridge
#            (turns ``ClaudeAgentOptions.permission_mode`` into a
#            ``can_use_tool`` async callback that parks pending decisions on
#            the host until ``submit_action`` resolves them).
#   c6ec0fd  feat(approvals): Slice 2 — approval API + orchestrator validation
#            + startup scan that seals stranded pendings on host restart.
#   9d75e87  refactor(approvals): Slice 1 — tighten ``permission_mode`` enum
#            to ``default`` / ``auto_review`` / ``full_access``; sink the
#            field from agent to session (per-session immutability post
#            create); drop legacy ``RequiresAction`` / ``PendingAction`` /
#            ``RuntimePort.resume(session, actions)`` path.
#
#   ╳ BREAKING (schema): kernel adds revision ``807642401b71`` chained off
#      ``14b5c6e20476`` that
#        • coerces ``agents.permission_mode`` rows from the legacy 5-value
#          enum (``default`` / ``accept_edits`` / ``plan`` / ``deny_all`` /
#          ``bypass``) to ``full_access`` and re-emits the CHECK with the
#          new 3-value enum (``default`` / ``auto_review`` / ``full_access``);
#        • adds a NOT NULL ``sessions.permission_mode`` column (default
#          ``full_access``) with the same 3-value CHECK.
#      Reversible (down migration restores schema, not row semantics).
#      Host hazard: dev DB stamped at ``14b5c6e20476`` will upgrade in
#      place via the chained revision; ``m000_legacy_dropout`` adds a
#      column-presence fingerprint on ``sessions.permission_mode`` as a
#      belt-and-braces drop-trigger for any DB whose alembic chain got
#      out of sync.
#
#   ╳ BREAKING (Session shape): ``Session`` gains a required
#      ``permission_mode: Literal["default","auto_review","full_access"]``
#      field defaulting to ``"full_access"``. Every host site that
#      constructs ``Session(...)`` or shallow-copies one MUST forward this
#      field or the next save coerces back to the default and silently
#      drops user-configured approval modes. Touched in valuz:
#        · adapters/system_prompt_builder.py (docstring only)
#        · modules/sessions/service.py — ``_copy_session`` field list,
#          ``SessionDetail`` / ``SessionListItem`` shape, ``create_session``
#          now stamps the session-level permission_mode from agent default.
#        · modules/sessions/recovery.py — ``_finalise_one`` preserves
#          ``permission_mode`` on the status copy.
#        · modules/projects/service.py — synthetic agents now carry
#          ``permission_mode="full_access"`` (was ``"bypass"`` — the legacy
#          5-value enum's bypass-everything role; the new 3-value enum's
#          equivalent is ``full_access``).
#
#   ╳ NEW API: ``POST /api/v1/sessions/{id}/actions`` + matching
#      ``SubmitActionRequest`` / ``SubmitActionResponse`` schemas resolve
#      a pending ``requires_action`` event with
#      ``{pending_id, decision: approve|reject|answer, message?, answers?}``.
#      Idempotent on (pending_id, decision); conflicting decisions return
#      409 with the original timestamp.
#      Host hazard: valuz frontend talks to its own ``/v1/sessions`` prefix,
#      so the host exposes a thin façade at
#      ``POST /v1/sessions/{id}/actions`` that delegates to
#      ``orchestrator.submit_action``. The kernel route is also mounted
#      under ``/api/v1/...`` — both reach the same orchestrator instance.
#
#   ╳ NEW EVENTS: ``requires_action`` (mid-turn, blocking) and
#      ``action_resolved`` (decision landed; ``decision`` ∈
#      ``approve|reject|answer|expired|interrupted``). Both carry
#      ``pending_id`` + ``message_id``; ``requires_action`` also carries
#      ``subject`` (``shell_command|file_change|mcp_tool_call|tool_input
#      |clarifying_questions``) and the available decisions list.
#      Host hazard: ``adapters/event_sse_adapter`` adds translations to
#      ``session.requires_action`` / ``session.action_resolved`` legacy
#      event types so the SSE consumer sees them without a frontend
#      regen. The deltas previously suppressed (``*_delta``) are still
#      filtered.
#
#   ╳ NEW HOOK: ``SessionOrchestrator.scan_orphan_pendings()`` should be
#      called at host startup to seal every ``requires_action`` event that
#      was still open when the previous host process died. Wired in
#      ``api/app.py`` after ``recover_running_sessions``.
#
#   eaad9b3  chore(events): skip persisting all *_delta event types
#   2da8027  fix(codex): omit model kwarg when session.model is empty
#   8a142c2  chore(deepagents): document DeepSeek thinking-disable workaround

# Notable changes since d5f2238 (kept for context):
#   d66241e  feat: dispatch runtimes via explicit Session.runtime_provider

# Notable changes since d22d8ab (kept for context):
#   0e15a2f  feat: add UserMessage.additional_context for upstream-injected
#            prompt context

# Notable changes since 7e60d39 (kept for context):
#   e8d6c87  feat: decouple agents from projects

# Notable changes since fd068a6 (kept for context):
#   cc85e65  feat(orchestrator): allow multiple concurrent sessions per project
#   c76e2cc  feat(codex): surface MCP startup status so failures stop being silent
#   27e08fc  fix(mcp): inherit parent env for stdio MCP child when user supplied none
#   5f82147  feat(codex): wire stdio MCP via TOML config_overrides
#   4593eaa  feat(deepagents): wire stdio MCP servers via shared env_vars resolver
#   fb1ac2e  feat(claude): wire stdio MCP servers + shared env_vars resolver
#   471a18a  feat(api): persist stdio MCP config + split OpenAPI schema into oneOf
#   eeebb74  feat(types): split McpServerConfig into tagged union + add Stdio variant
#   f238b7f  feat(codex): add CodexRuntime — third runtime via OpenAI Codex SDK (dormant here)
#   06c11f9  feat(codex): expose Session.toolkit to Codex via MCP-over-HTTP (not mounted here)
#   6c1a957  feat(events): add tool_input_delta + tool_output_delta with keyed coalescing
#   d7c8d09  feat(claude): emit tool_input_delta from input_json_delta StreamEvent
#   8b5170f  chore(coalesce): expose flush_ms via DELTA_COALESCE_FLUSH_MS env
#   eb73cb8  fix(deepagents): tolerate tool exceptions via ToolErrorTolerantMiddleware

# DO NOT EDIT files under backend/kernel/ directly.
# To upgrade:
#   cd /Users/river/Documents/xb_workspace/agent-harness && git pull
#   git rev-parse HEAD  # update commit above
#   rsync -a --delete agent-harness/backend/{src,app,alembic} valuz-agent/backend/kernel/
#   cp agent-harness/backend/alembic.ini valuz-agent/backend/kernel/alembic.ini
#   make test-all && make typecheck
# Any kernel changes belong upstream first; never patch this directory.
